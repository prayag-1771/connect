<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Framework theme rather than a Material Components one: the UI is entirely
      Compose, so pulling in the View-based Material library would add a
      dependency nothing renders with.
    -->
    <style name="Theme.Connect" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!--
      The quick reply strip. Translucent and dimmed so the home screen stays
      visible behind it, which is what makes it read as a peek rather than as
      having opened the app.
    -->
    <style name="Theme.Connect.QuickReply" parent="@android:style/Theme.Material.Dialog.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimAmount">0.35</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!--
      The drawing overlay. No dim at all: the strokes are meant to sit on top
      of whatever is already on screen, so anything behind them stays exactly
      as it was.
    -->
    <style name="Theme.Connect.Overlay" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimAmount">0.0</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">false</item>
    </style>
</resources>
